# include "main.h"
# include <stdio.h>

/**
 * _puts - A function that prints a string followed by a new line
 * @str: A pointer that is being returned
 * Return: A string followed by a new line
 */
void _puts(char *str)
{
	puts(str);
}
