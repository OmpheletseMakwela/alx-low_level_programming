#include "main.h"
#include <stdio.h>

/**
 * _puts_recursion - A prototype that prints a string followed by a new line
 * @s: A pointer that hold an adress of a string
 */
void _puts_recursion(char *s)
{
	puts(s);
	printf("\n");
}

