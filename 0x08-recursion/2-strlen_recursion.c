#include "main.h"
#include <string.h>

/**
 * _strlen_recursion - A prototype that returns the length of a string
 * @s: A pointer that contains the address of a pointer
 * Return: the length of a string
 */
int _strlen_recursion(char *s)
{
	int r;

	r = strlen(s);
	return (r);
}
